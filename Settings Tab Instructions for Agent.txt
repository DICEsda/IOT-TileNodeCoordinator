Create a new top-level “Settings” tab in my SmartTile frontend.
This tab manages system-wide environment configuration based on my .env file.
Have a password to access ( the password should be 1234) sensitive secrets such as MongoDB passwords, JWT keys, Google client secrets, ESP-NOW keys, or API keys. Only expose safe, meaningful parameters for runtime control.
Structure the Settings tab into these sections:
1. System Identity
– DEFAULT_SITE_ID
– DEFAULT_COORD_ID
– MAX_COORDINATORS_PER_SITE
– MAX_NODES_PER_COORDINATOR

2. Network & Connectivity
– ESP32_WIFI_SSID
– ESP32_WIFI_PASSWORD
– ESP32_WIFI_TIMEOUT
– ESP32_RECONNECT_DELAY
– ESP32_PING_INTERVAL
– Display API_URL and WS_URL as read-only fields

3. MQTT Behavior
– MQTT_QOS
– MQTT_KEEPALIVE
– MQTT_CLIENT_ID

4. Coordinator Configuration
– ESP32_FIRMWARE_VERSION (read-only)
– ESP32_OTA_URL
– COORD_LED_COUNT
– COORD_BUTTON_HOLD_TIME
– COORD_MAX_NODES

5. Node Defaults
– NODE_LED_COUNT
– NODE_DEFAULT_BRIGHTNESS
– NODE_TEMP_SENSOR_ENABLED
– NODE_BUTTON_ENABLED

6. Zones
– DEFAULT_ZONES (editable list)
– ALLOW_CUSTOM_ZONES
– MAX_ZONES

7. Energy & Automation
– ENERGY_SAVING_MODE
– AUTO_OFF_DELAY
– MOTION_SENSITIVITY
– DEFAULT_LIGHT_INTENSITY

8. Telemetry & Logging
– TELEMETRY_RETENTION_DAYS
– DEBUG_MODE
– METRICS_INTERVAL
– LOG_TO_FILE

9. Developer Tools (only visible if a toggle “Enable Developer Mode” is activated)
– MOCK_DEVICES_ENABLED
– SIMULATOR_ENABLED
– SIMULATOR_INTERVAL

Backend / MQTT / WebSocket behavior:
• Every parameter change must update the backend through a REST endpoint.
• Backend persists settings to MongoDB.
• Backend publishes relevant changes over MQTT so devices update live.
• Frontend subscribes to real-time updates via WebSocket to stay in sync.
• Provide all required TypeScript interfaces, schemas, and Angular components.
• Provide matching backend route handlers, MQTT topics, and DB models.

Try your best to make it dynamic, since im not sure how many coordinators, or nodes per coordinator ill be using.